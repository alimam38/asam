This folder is a production repository and the single source of truth for one product. Standing rules for every session:

- Read README.md and GATES.md before starting any task; all work serves the currently open gate.
- Plain Markdown, kebab-case filenames. Copy, never move or delete, my original files.
- Commit in logical units with meaningful messages and update CHANGELOG.md when work lands.
- Report only what exists on disk. If a step failed or was skipped, say so plainly. If a fact is unknown, write "Not yet defined" plus the question that would define it.
- Pause only when a decision genuinely needs me — destructive actions, scope changes, or input only I can provide.
