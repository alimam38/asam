# meridia_core snapshot — 2026-08-08

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Nightly cloud job ran at ~12:43 UTC from a fresh sandbox.
- Credentials fetched from Dropbox `/Claude/.env` — all 5 keys present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from Cloudflare apt pool; `access tcp --hostname db.meridiahq.com` launched with service-token env vars.
- Two independent tunnel attempts (local ports 15432 and 15433), ~8–10 s warmup each. Both times pg8000 got `ConnectionResetError: [Errno 104] Connection reset by peer`, and cfd.log shows:

```
ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
```

## Failure classification

`websocket: bad handshake` = the Cloudflare edge rejected the Access websocket upgrade **before** any NAS-side connectivity was exercised. Per the runbook this is the **Cloudflare Access/edge layer** class: service token invalid/expired/not authorized for the `db.meridiahq.com` Access application, or DNS/edge propagation for that hostname. It is NOT a NAS routing failure (that would show connect-then-reset from the connector) and NOT a DB credential failure (Postgres auth was never reached).

## Data

- Server version: n/a (not connected)
- Public table count: n/a
- Top-25 tables: n/a — no snapshot data fabricated.

## Suggested next check

Verify the Cloudflare Access service token (rotation/expiry) and that it is listed in the Access policy for the `db.meridiahq.com` application.
