# meridia_core snapshot — 2026-09-24

Run: 2026-09-24 12:35 UTC, fresh cloud session, via Cloudflare Access tunnel `db.meridiahq.com` (cloudflared 2026.9.1, pg8000 1.31.5). Read-only.

## Connection status: FAIL — tunnel/origin side (Access layer passed)

- Credentials: all 5 keys present in `/Apps/Cl Cowork/Cowork-Master/.env` (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- pg8000: `ConnectionResetError: [Errno 104] Connection reset by peer` (never reached Postgres auth).

### Why the bad handshake is NOT a token problem

Direct HTTPS probes of db.meridiahq.com from the sandbox:

| Probe | Result |
|---|---|
| No Access headers | **403** (Access correctly blocks) |
| With CF-Access service token | **502** `error code: 502` (server: cloudflare, cf-ray a401d52a4e75e5c1-IAD) |
| With token + WebSocket upgrade | **502** |

The service token is accepted (403 → 502 once the token is presented), DNS resolves to Cloudflare. The 502 is returned by the Cloudflare edge after Access, meaning the tunnel connector for db.meridiahq.com could not deliver the request to its origin — i.e. the cloudflared connector on the NAS side is down/unhealthy or cannot reach 192.168.0.160:5433 (e.g. cloudflared in Docker without host networking, wrong service URL/protocol in the tunnel's public-hostname config, or Postgres not listening).

**Failure class:** Access edge handshake fails with HTTP 502 → tunnel connector ↔ origin (NAS-side), not service token, not DB credentials.

## Server version

n/a (not connected)

## Public table count

n/a (not connected)

## Top 25 tables by live rows

n/a (not connected) — no data fabricated.

## Fix for Ali

1. On the NAS, confirm the cloudflared container/service is running and shows healthy connections in the Cloudflare Zero Trust dashboard (Networks → Tunnels).
2. Check the tunnel's public hostname db.meridiahq.com → service `tcp://192.168.0.160:5433` (type TCP, not HTTP).
3. If cloudflared runs in Docker, use `--network host` (or ensure the container can route to 192.168.0.160:5433); test with `nc -zv 192.168.0.160 5433` from inside the container.
