# meridia-core snapshot — 2026-07-27

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run: nightly cloud connectivity + snapshot job, 2026-07-27 ~12:39 UTC
- cloudflared 2026.7.3 installed from Cloudflare apt pool; pg8000 client ready.
- Credentials fetched from Dropbox `/Claude/.env` — all 5 variables present (CF service token + DB user/password/name).
- Tunnel attempt 1: `cloudflared access tcp --hostname db.meridiahq.com` → `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`; client saw `Connection reset by peer` on 127.0.0.1:15432.
- Tunnel attempt 2 (fresh listener, 12s warm-up): identical `websocket: bad handshake`.

**Failure class:** Cloudflare Access/edge layer — the Access edge rejected the websocket upgrade before any tunnel traffic reached the NAS. Per runbook this means either the service token (CF_ACCESS_CLIENT_ID/SECRET) is invalid/expired/not authorized for the db.meridiahq.com Access application, or DNS/edge propagation for that hostname is broken. The DB itself and NAS-side routing were never reached, so no conclusion about them.

**Server version:** n/a (not connected)
**Public table count:** n/a
**Top-25 tables:** n/a

No data was fabricated; read-only job made no changes to meridia_core.
