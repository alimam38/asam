# meridia_core snapshot — 2026-08-09

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Run detail

- Credentials fetched from Dropbox `/Claude/.env` — all 5 required variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from pkg.cloudflare.com apt pool; pg8000 1.31.5 installed.
- Tunnel launched: `cloudflared access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432` with service-token env vars set.
- Two independent tunnel launches, three connection attempts total (~12s and ~22s after launch). Every attempt failed identically:

```
ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
```

- Local pg8000 connect saw `ConnectionResetError: [Errno 104] Connection reset by peer` — the local listener reset because the edge handshake never completed. No Postgres-level auth was reached.

## Failure classification

`websocket: bad handshake` = **Cloudflare Access / edge layer**. Per runbook, this points to the service token (invalid, expired, or not authorized in the Access policy for db.meridiahq.com) or DNS/edge propagation — NOT NAS-side routing (that would present as connect-then-reset/timeout after a successful handshake) and NOT DB credentials (never reached the DB).

## Snapshot data

None — no data collected this run. Server version, public table count, and top-25 tables unavailable (connection failed at the edge). Nothing fabricated.

## Suggested next checks

1. Cloudflare Zero Trust → Access → Service Auth: confirm the service token exists, is not expired, and its Client ID matches the one in `/Claude/.env`.
2. Access application for `db.meridiahq.com`: confirm a policy with action *Service Auth* includes this token.
3. Confirm the `db.meridiahq.com` hostname is still routed to the tunnel (cloudflared config / DNS CNAME).
