# meridia_core snapshot — 2026-08-24

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly cloud connectivity + snapshot job (fresh cloud sandbox), 2026-08-24 ~12:43 UTC
- Credentials fetched from Dropbox `/Claude/.env`: all 5 variables present and non-empty (CF Access service token pair + DB user/password/name)
- cloudflared 2026.8.2 installed from Cloudflare apt pool; local TCP listener started OK
- Tunnel attempt to `db.meridiahq.com`: **failed twice** (two separate tunnel instances, ports 15432 and 15433) with:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- Client side saw `ConnectionResetError: [Errno 104] Connection reset by peer` at the Postgres layer (expected consequence — the tunnel never established)

## Failure classification

`websocket: bad handshake` = **Cloudflare Access/edge layer** — the request is being rejected before reaching the NAS-side connector. Likely causes: service token invalid/expired/not authorized by the Access application for `db.meridiahq.com`, or DNS/edge propagation issue for the hostname.

Not indicated: NAS-side routing (would show connect-then-reset at tunnel level) or DB credentials (connection never reached Postgres auth).

## Snapshot data

None captured — no fabricated data. Server version, public table count, and top-25 tables unavailable this run.
