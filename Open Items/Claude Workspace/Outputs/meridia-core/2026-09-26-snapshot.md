# meridia-core snapshot — 2026-09-26

Run: 2026-09-26 ~12:36 UTC, nightly scheduled job, fresh cloud session (cloudflared 2026.9.3, pg8000 1.31.5).

## Connection status: FAILED — `websocket: bad handshake` (Access passed; edge returns 502 from the tunnel/origin hop)

- Credentials: all five variables present in `/Apps/Cl Cowork/Cowork-Master/.env` (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared log: `Start Websocket listener host=127.0.0.1:15432` then `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`.
- pg8000 client: `ConnectionResetError: [Errno 104] Connection reset by peer`.
- No database session was established. No queries ran.

## Extra diagnostic

- DNS: db.meridiahq.com resolves to Cloudflare (2606:4700:3035::ac43:d1d9, 2606:4700:3033::6815:1754).
- Plain GET, no token: HTTP 403 (Access blocks). With service-token headers: HTTP 502.
- WebSocket upgrade request (same handshake cloudflared performs), no token: 403. With token: `HTTP/1.1 502 Bad Gateway`, body `error code: 502` (cf-ray a41250b10bb228bb-IAD).
- Reading: the service token is accepted by Access (403 → 502). The bad handshake is Cloudflare's edge answering the WebSocket upgrade with 502, meaning the tunnel/origin hop behind Access is failing: the connector can't reach 192.168.0.160:5433, or the hostname isn't routed to a healthy tunnel with a `tcp://` ingress. A fully offline tunnel usually shows 530/1033, so a live connector that can't reach Postgres is the more likely case.

## Server version

n/a (not connected)

## Public table count

n/a (not connected)

## Top 25 tables by live row count

n/a (not connected). No data fabricated.

## Trend

Every snapshot in this folder, 2026-07-23 through today, records a failed connection. There is no successful run on record.

## Suggested check for Ali

1. Cloudflare Zero Trust → Networks → Tunnels: is the tunnel for db.meridiahq.com HEALTHY with an active connector?
2. Its public-hostname rule for db.meridiahq.com should be `tcp://192.168.0.160:5433`. From the NAS, confirm the connector can reach that address and port (if cloudflared runs in Docker, use `--network host`).
3. Confirm Postgres is listening on 5433 and accepts connections from the connector's address (listen_addresses / pg_hba.conf).
