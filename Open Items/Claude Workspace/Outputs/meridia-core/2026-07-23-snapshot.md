# meridia_core snapshot — 2026-07-23

**Connection status:** FAILED — NAS-side: tunnel connector cannot reach the origin (edge auth OK, 502 from edge)

Two runs today, both failed with client-side `websocket: bad handshake` (nightly run ~07:34 UTC ×2 attempts; manual test fire ~07:35 UTC ×1). The manual fire added edge probes that refine the classification:

- `curl https://db.meridiahq.com` WITHOUT the service token → **403 Cloudflare Access error page** — DNS/edge routing works and Access is enforcing the policy.
- `curl https://db.meridiahq.com` WITH CF-Access-Client-Id/Secret headers → **502 ("error code: 502")** from the Cloudflare edge — the service token is ACCEPTED (no 403), but the edge gets a bad gateway trying to reach the origin.

**Refined failure classification:** NOT the service token and NOT DNS/edge propagation. Cloudflare authenticates the request and then cannot complete the connection through the tunnel to the origin — i.e. the cloudflared connector on the NAS is either not connected to Cloudflare or cannot reach `192.168.0.160:5433`. Per the runbook, the likely culprit is cloudflared running in Docker without host networking (or the connector/origin service being down). The client-side `websocket: bad handshake` is the downstream symptom of the edge's 502.

- Setup-session context (from the manual fire): config chain was verified via the Cloudflare API (ingress, DNS CNAME, Access app + service token all correct) — consistent with the 403/502 probe results above.
- cloudflared 2026.7.2, pg8000 1.31.5; credentials from Dropbox /Claude/.env all present.

**Data:** none collected — no server version, table count, or row counts (not fabricated).

**Suggested checks (NAS side):** confirm the cloudflared tunnel connector shows HEALTHY/connected in Cloudflare Zero Trust; if it's in Docker, verify it can reach 192.168.0.160:5433 (host networking or correct bridge routing); confirm Postgres is listening on 5433 and reachable from the connector's network namespace.
