# meridia_core snapshot — 2026-07-26

**Connection status: FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)**

No data was read from the database. Server version, table count, and row counts are unavailable for this run.

## Diagnostics (nightly cloud job, 12:40 UTC)

- Credentials fetched from Dropbox `/Claude/.env` — all 5 keys present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from pkg.cloudflare.com; tunnel launched with `TUNNEL_SERVICE_TOKEN_ID`/`TUNNEL_SERVICE_TOKEN_SECRET` exported.
- Two independent tunnel attempts (ports 15432 and 15434), ~10s warm-up each. Both failed identically:
  - `cfd.log`: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  - pg8000 client saw `ConnectionResetError: [Errno 104] Connection reset by peer` on the local forwarder.
- Edge probe: `curl https://db.meridiahq.com` with CF-Access service-token headers returned **HTTP 502** (not 403). DNS resolved and the Cloudflare edge answered.

## Interpretation

Per the runbook, `websocket: bad handshake` classifies as **Cloudflare Access/edge layer**. The 502 (rather than a 403 Access-denied) narrows it further: the edge accepted the request but found **no healthy tunnel origin** behind `db.meridiahq.com` — most likely the cloudflared tunnel connector on the NAS is down or disconnected. Service-token rejection would normally surface as 403.

## Suggested check

On the NAS: confirm the cloudflared connector container/service is running and connected (`cloudflared tunnel info`, or the Zero Trust dashboard → Tunnels → connector status).

---
*Read-only job; no queries executed against meridia_core (connection never established).*
