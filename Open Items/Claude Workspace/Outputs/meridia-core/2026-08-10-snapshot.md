# meridia-core snapshot — 2026-08-10

**Connection status: FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)**

No data was read from the database. Server version, table count, and top-25 tables are unavailable for this run.

## Run details

- Job ran from a fresh cloud sandbox at ~12:40–12:55 UTC.
- Credentials fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from pkg.cloudflare.com apt pool.
- `cloudflared access tcp --hostname db.meridiahq.com` launched with service token env vars; listener started on 127.0.0.1:15432.
- Three fresh tunnel attempts (12:43, 12:50 UTC), each: pg8000 connect → `ConnectionResetError: [Errno 104] Connection reset by peer`.
- cfd.log on every attempt:

```
ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
```

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access/edge layer** — the tunnel client never reached the NAS-side connector. Likely causes: the service token in `/Claude/.env` is invalid/expired/not authorized by the Access application policy for db.meridiahq.com, or DNS/edge propagation for the hostname. This is NOT a DB-credential or NAS-side-routing failure (those present differently: auth error after connect, or connect-then-timeout).

## Suggested checks

1. Cloudflare Zero Trust → Access → Service Auth: confirm the token exists, is not expired, and is included in the Access app policy for db.meridiahq.com.
2. Confirm db.meridiahq.com resolves to Cloudflare edge and the Access application covers TCP.
3. Re-run this job after rotating/fixing the token.
