# Meridia Core Snapshot — 2026-09-20

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly connectivity + snapshot job, fresh cloud session, 2026-09-20 ~12:38 UTC
- Credentials: fetched from hub `.env` OK (CF_ACCESS_CLIENT_ID/SECRET, NAS_DB_USER/PASSWORD/NAME all present, non-empty)
- cloudflared 2026.9.1 installed from Cloudflare apt pool OK
- Tunnel attempt: `cloudflared access tcp --hostname db.meridiahq.com` with service-token env vars
- cfd.log (both attempts, ports 15432 and 15433):
  - `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- Client-side symptom: pg8000 connect → `ConnectionResetError [Errno 104] Connection reset by peer`
- Retried once on a fresh local port after 12s warmup — identical failure

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access/edge layer** — service token rejected by the Access policy, or DNS/edge propagation for db.meridiahq.com. Not a NAS-side routing issue (that would present as connect-then-reset AFTER a successful handshake) and not a DB credential issue (connection never reached Postgres auth).

## Data

- Server version: n/a (not reached)
- Public table count: n/a
- Top-25 tables by live rows: n/a

No data fabricated; snapshot intentionally empty pending Access fix.
