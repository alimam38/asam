# meridia_core snapshot — 2026-07-23

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run: nightly cloud connectivity + snapshot job, 2026-07-23 ~07:34 UTC
- cloudflared 2026.7.2 installed OK (Cloudflare apt pool); pg8000 client OK
- Credentials fetched from Dropbox /Claude/.env — all 5 vars present (CF_ACCESS_CLIENT_ID/SECRET, NAS_DB_USER/PASSWORD/NAME)
- `cloudflared access tcp --hostname db.meridiahq.com` failed twice (two separate tunnel attempts, ~15s apart):
  - `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  - Local pg connect then got `Connection reset by peer` (expected consequence — the tunnel never established)

**Failure classification:** Cloudflare Access/edge layer — service token rejected or DNS/edge propagation issue for db.meridiahq.com. The failure occurs before any NAS-side or Postgres-auth stage, so DB credentials and NAS routing were not exercised.

**Data:** none collected — no server version, table count, or row counts (not fabricated).

**Suggested checks:** verify the Access application + service token for db.meridiahq.com in Cloudflare Zero Trust (token not expired/revoked, policy includes the service token), and that the hostname's DNS/edge config still routes to the tunnel.
