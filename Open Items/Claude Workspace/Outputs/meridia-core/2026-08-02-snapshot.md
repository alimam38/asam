# meridia_core snapshot — 2026-08-02

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly cloud connectivity + snapshot job (fresh cloud session)
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared: 2026.7.3 installed from Cloudflare apt pool
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com` started; local listener OK
- Attempt 1 (12:43:32Z) and attempt 2 (12:44:04Z) both failed identically:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- Client-side symptom: `ConnectionResetError [Errno 104] Connection reset by peer` on 127.0.0.1 tunnel port

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access / edge layer** — service token rejected or DNS/edge propagation issue for `db.meridiahq.com`. The failure occurs before any NAS-side or Postgres-auth step, so no data snapshot was possible this run.

- Server version: n/a (not reached)
- Public table count: n/a
- Top-25 tables by live rows: n/a

No queries were executed against meridia_core (read-only job; nothing modified).
