# meridia_core snapshot — 2026-08-17

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly connectivity + snapshot job, fresh cloud sandbox, 2026-08-17 ~15:46 UTC.
- Credentials fetched from Dropbox `/Claude/.env`: all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.8.2 installed from Cloudflare apt pool; pg8000 client OK.
- `cloudflared access tcp --hostname db.meridiahq.com` started; local listener came up, but on connect cloudflared logged:

  ```
  ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
  ```

  and the Postgres client saw `ConnectionResetError [Errno 104] Connection reset by peer`.
- Reproduced twice (8s and 12s tunnel warm-up, separate ports) — not transient.

## Failure classification

`websocket: bad handshake` = the Cloudflare Access/edge layer rejected the tunnel connection **before** any NAS-side or database involvement. Likely causes: the service token (CF_ACCESS_CLIENT_ID/SECRET) is invalid, expired, or not authorized for the `db.meridiahq.com` Access application; or the hostname/edge configuration hasn't propagated.

Not a DB-credential issue (never reached Postgres auth) and not NAS-side routing (that failure mode is connect-then-reset/timeout after a successful handshake).

## Snapshot data

None collected — no data fabricated. Server version, table count, and top-25 tables unavailable this run.
