# meridia-core snapshot — 2026-08-31

**Run:** nightly connectivity + snapshot job, cloud sandbox → Cloudflare Access tunnel → `db.meridiahq.com`
**Run time:** 2026-08-31 11:51 UTC
**Connection status:** FAIL — `websocket: bad handshake` (Cloudflare Access / edge layer)

## Diagnostic detail

| Check | Result |
|---|---|
| Credentials from Dropbox `/Claude/.env` | All 5 present (CF id/secret, DB user/password/name) |
| cloudflared | 2026.8.3 from pkg.cloudflare.com apt pool |
| DNS `db.meridiahq.com` | Resolves to Cloudflare anycast (2606:4700:…) |
| `cloudflared access tcp` | `ERR failed to connect to origin error="websocket: bad handshake"` |
| pg8000 connect via 127.0.0.1:15432 | `ConnectionResetError: [Errno 104] Connection reset by peer` (expected — local listener had no upstream) |
| HTTPS GET **without** service token | `403` (Access is enforcing) |
| HTTPS GET **with** service token | `502` from `server: cloudflare`, body `error code: 502` |

**Interpretation:** the service token is being accepted by Cloudflare Access (403 → 502 once the token is added), so this is not a credentials problem. The 502 comes from the Cloudflare edge itself, which means the edge has no healthy tunnel connector to hand the request to — i.e. the `cloudflared` tunnel connector on the NAS side is down, not registered for the `db.meridiahq.com` public hostname, or the tunnel route is misconfigured. The NAS-side routing to `192.168.0.160:5433` cannot be evaluated until the connector is reachable.

**Suggested next check (NAS side):** `cloudflared tunnel info <tunnel>` / Zero Trust → Networks → Tunnels: confirm the tunnel shows a HEALTHY connector and that `db.meridiahq.com` → `tcp://192.168.0.160:5433` is in its public-hostname routes.

## Snapshot data

Not collected — no database connection this run. No data fabricated.

- Server version: n/a
- Public table count: n/a
- Top-25 tables by live rows: n/a

---

## Run 2 — 2026-08-31 12:40 UTC

**Connection status:** FAIL — same class as run 1. Unchanged.

- Credentials fetched from Dropbox `/Claude/.env`; all 5 vars present.
- cloudflared 2026.8.3; `access tcp` → `websocket: bad handshake` on 2/2 attempts (20 s apart); pg8000 saw `Connection reset by peer`.
- Edge probe: 403 without service token → **502** with service token (`server: cloudflare`, `error code: 502`).

**Classification:** Access accepts the token; failure is at the Tunnel. A 502 (rather than 530/1033) usually means a connector *is* registered but could not dial its origin — pointing at the NAS-side path to `192.168.0.160:5433` (cloudflared in Docker without host networking, or Postgres not listening on 5433/that interface). Either way the fix is on the NAS, not in `.env`.

**Streak:** every nightly run since at least 2026-08-27 (5 consecutive days) has failed with this same class. DB credentials remain unverified — no TCP path has reached Postgres.

No queries executed; no data fabricated.
