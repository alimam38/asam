# meridia_core snapshot — 2026-09-10

- **Run**: nightly connectivity + snapshot job (cloud session), 12:38–12:40 UTC
- **Connection status**: FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)
- **Failure class**: service token rejected at the Access edge, or DNS/edge propagation for db.meridiahq.com. The tunnel client (cloudflared 2026.9.0) started its local listener normally; every origin dial to https://db.meridiahq.com was refused at the websocket handshake (3 attempts across 2 clean runs, ~12:39–12:40 UTC). The failure is BEFORE the NAS/connector — this is not the 192.168.0.160:5433 routing case (that presents as connect-then-reset after a successful handshake) and not a DB auth error (no Postgres auth exchange ever occurred).
- **Server version**: n/a (never connected)
- **Public table count**: n/a
- **Top-25 tables**: n/a

## Detail
- Credentials fetched from Dropbox hub .env — all 5 keys present (CF_ACCESS_CLIENT_ID/SECRET, NAS_DB_USER/PASSWORD/NAME); values not printed.
- cloudflared installed from Cloudflare apt pool (2026.9.0), pg8000 1.31.5.
- Client-side symptom: `ConnectionResetError [Errno 104]` on 127.0.0.1:15432 (local listener resets after edge handshake fails).
- cfd.log: `ERR failed to connect to origin error=\"websocket: bad handshake\" originURL=https://db.meridiahq.com` — repeated on retry.

## Suggested fix (for Ali)
Verify the Cloudflare Access service token (CF_ACCESS_CLIENT_ID/SECRET in the hub .env) is still valid and attached to the Access application for db.meridiahq.com; check the Access app/tunnel public hostname config in the Cloudflare dashboard.

*No data fabricated; no writes made to meridia_core (never connected).*
