# meridia_core snapshot — 2026-07-29

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly cloud connectivity + snapshot job, 2026-07-29 ~12:39 UTC, fresh cloud sandbox.
- Credentials fetched from Dropbox `/Claude/.env`: all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from pkg.cloudflare.com apt pool; pg8000 client ready.
- Tunnel attempt: `cloudflared access tcp --hostname db.meridiahq.com` with service-token env vars.
- Result (reproduced on 2 independent attempts, ~10s apart on fresh local ports):
  - cfd.log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  - Client side: `ConnectionResetError: [Errno 104] Connection reset by peer` before Postgres handshake.

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access / edge layer** — either the service token (CF_ACCESS_CLIENT_ID/SECRET) is not accepted by the Access application for db.meridiahq.com, or DNS/edge propagation for the hostname is not in place. The failure occurs before any traffic reaches the NAS or Postgres, so nothing can be said about the DB itself tonight.

- Server version: n/a (no connection)
- Public table count: n/a
- Top-25 tables by live rows: n/a

No data was fabricated; no writes were made to meridia_core.
