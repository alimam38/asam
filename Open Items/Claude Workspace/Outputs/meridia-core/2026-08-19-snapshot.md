# meridia-core snapshot — 2026-08-19

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly connectivity + snapshot job (cloud session), 2026-08-19 ~12:38 UTC
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared: 2026.8.2 (installed from pkg.cloudflare.com apt pool)
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432`
- Attempt 1 (12:38:32Z): `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com` → pg8000 got `ConnectionResetError(104, 'Connection reset by peer')`
- Attempt 2 (12:39:05Z, retry with longer warm-up): identical failure — not transient

## Failure classification

`websocket: bad handshake` = the Cloudflare Access/edge layer rejected the connection **before** reaching the tunnel connector. Likely causes: service token invalid/expired/not authorized by the Access application for db.meridiahq.com, or DNS/edge propagation for the hostname. This is NOT a NAS-side routing issue (that would present as connect-then-reset/timeout) and NOT a DB credential issue (the tunnel never reached Postgres).

## Snapshot data

None collected — no database connection was established. Server version, public table count, and top-25 tables unavailable this run. No data fabricated.
