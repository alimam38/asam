# meridia_core nightly snapshot — 2026-09-02

**Connection status:** FAILED — Cloudflare Access / edge layer
**Failure class:** `websocket: bad handshake` (service token invalid/expired, or DNS/edge propagation for db.meridiahq.com)

## Run details
- Session: fresh cloud-only run (no local device).
- cloudflared: v2026.8.3, installed from Cloudflare apt pool.
- Local Access listener started OK on 127.0.0.1:15432; edge refused the websocket upgrade to origin `https://db.meridiahq.com`.
- Postgres client (pg8000) then saw `ConnectionResetError [Errno 104]` connecting to the dead local listener — consistent with the edge handshake failure above, not a DB-side reset.
- Credentials (.env): all present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME) — so this is not a missing-secret failure.

### cloudflared log
```
Start Websocket listener host=127.0.0.1:15432
ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
```

## Snapshot data
Not collected — the tunnel never reached the database, so no query ran. No data fabricated.

- Server version: n/a (unreachable)
- Public table count: n/a (unreachable)
- Top 25 tables by live row count: n/a (unreachable)

## Interpretation
`websocket: bad handshake` is an Access/edge-layer rejection, distinct from a DB auth error (would show a password/auth message) or a NAS-side routing failure (would connect then reset/timeout). Most likely the CF Access service token is invalid or expired, or the Access application / DNS for `db.meridiahq.com` has not propagated at the edge. Next check: verify the service token is still active in the Cloudflare Access dashboard and that the `db.meridiahq.com` Access app + hostname route are published.
