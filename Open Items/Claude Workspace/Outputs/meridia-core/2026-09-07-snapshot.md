# meridia_core snapshot — 2026-09-07

**Connection status:** FAILED — Cloudflare edge reachable, tunnel origin down (HTTP 502)

## Failure classification

- `cloudflared access tcp` (v2026.8.3, fresh install from pkg.cloudflare.com) started its local listener normally, then logged `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com` on the connect attempt; the client saw `ConnectionResetError [Errno 104]`.
- Direct HTTPS probe of `https://db.meridiahq.com` with the CF-Access-Client-Id/Secret headers returned **HTTP 502, body `error code: 502`, `server: cloudflare`, cf-ray a375c7281a06fc18-IAD** — a Cloudflare edge response, not an Access 403/login redirect.
- Interpretation: DNS and the Cloudflare edge are fine, and the service token was not rejected by Access. The 502 means Cloudflare cannot reach the origin — the **NAS-side cloudflared tunnel connector for db.meridiahq.com appears to be down or not connected** (or the connector is up but cannot reach 192.168.0.160:5433, e.g. cloudflared in Docker without host networking). In the job's taxonomy this surfaces as the "websocket: bad handshake" class, but the 502 evidence points specifically at the tunnel/origin side rather than the service token.
- Retried on a clean tunnel process: same result. Credentials in /Cowork-Master/.env were all present and non-empty (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME); DB auth was never reached, so DB credentials are unverified this run.

## Snapshot data

None — no connection was established. No server version, table count, or row counts were collected (nothing fabricated).

**Suggested fix:** check that the cloudflared tunnel connector on the NAS is running and connected (Cloudflare Zero Trust → Tunnels should show it healthy), and that it can reach 192.168.0.160:5433 from inside its container.

---
Run: nightly connectivity + snapshot job, cloud session, 2026-09-07 12:39 UTC.
