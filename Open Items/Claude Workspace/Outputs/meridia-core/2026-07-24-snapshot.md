# meridia_core snapshot — 2026-07-24

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Job ran from a fresh cloud sandbox at 2026-07-24 ~12:39 UTC.
- Credentials fetched from Dropbox `/Claude/.env`: all 5 required variables present (CF service token ID/secret, DB user/password/name).
- cloudflared 2026.7.3 installed from Cloudflare apt pool; local Websocket listener bound on 127.0.0.1:15432 successfully.
- On each Postgres connection attempt, cloudflared reported:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  and the client saw `ConnectionResetError: [Errno 104] Connection reset by peer`.
- Reproduced twice (two separate clean tunnel launches, 12:39:05Z and 12:39:57Z).

## Classification

`websocket: bad handshake` ⇒ **Cloudflare Access/edge layer** — the edge rejected the tunnel handshake before any origin/DB traffic. Likely causes: the service token (CF_ACCESS_CLIENT_ID/SECRET) is invalid, expired, or not authorized in the Access policy for `db.meridiahq.com`, or the Access application/DNS record for that hostname is missing / not yet propagated.

Not reached: NAS-side routing (192.168.0.160:5433) and DB credentials were never exercised — no data on those layers this run.

## Snapshot data

None collected (no DB connection). Nothing was written to or read from meridia_core.
