# meridia_core snapshot — 2026-09-05

**Connection status: FAILED — NAS-side origin unreachable (tunnel connector down)**

Run at 2026-09-05 ~12:39 UTC from a fresh cloud session (nightly connectivity + snapshot job).

## Failure classification

- cloudflared `access tcp` log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com` (repeated on retry ~10s apart; client saw `Connection reset by peer`).
- Diagnostics run to refine the class:
  - DNS: `db.meridiahq.com` resolves to Cloudflare edge (172.67.209.217 / 104.21.23.84) — DNS/edge propagation is fine.
  - HTTPS GET **without** service token → **HTTP 403** (Access is enforcing — expected).
  - HTTPS GET **with** CF-Access-Client-Id/Secret headers → **HTTP 502** (token ACCEPTED; edge cannot reach origin).
- Conclusion: the service token in `Cowork-Master/.env` is valid and Cloudflare Access is authenticating it. The failure is on the NAS side: the cloudflared tunnel connector serving db.meridiahq.com is down or cannot reach 192.168.0.160:5433 (per runbook: likely cloudflared in Docker without host networking, or the connector container stopped). This matches the "tunnel connector cannot reach origin" class, surfacing as `websocket: bad handshake` + 502 at the edge.
- NOT a DB-credential issue (never reached Postgres auth). NOT a service-token issue (403→502 delta proves auth passes).

## Snapshot data

None collected — no fabrication. Server version, public table count, and top-25 tables omitted; last good snapshot remains the previous file in this folder.

## Suggested fix (Ali)

Check the NAS: is the cloudflared tunnel connector container running, and can it reach 192.168.0.160:5433? Restart the connector (host networking) and re-run this job or wait for tomorrow's 12:30 UTC run.
