# meridia_core snapshot — 2026-08-30

**Connection status: FAILED — Cloudflare Access/edge layer (websocket: bad handshake)**

Run: 2026-08-30 ~12:39 UTC, fresh cloud sandbox (nightly connectivity + snapshot job).

## What happened

- Credentials fetched from Dropbox `/Claude/.env` — all 5 variables present and non-empty (CF service token ID/secret, DB user/password/name).
- cloudflared 2026.8.2 installed from Cloudflare's apt pool; `access tcp --hostname db.meridiahq.com` launched with `TUNNEL_SERVICE_TOKEN_ID`/`TUNNEL_SERVICE_TOKEN_SECRET` set.
- Local listener started fine; on each connection attempt cloudflared logged:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- The pg8000 client saw `ConnectionResetError: [Errno 104] Connection reset by peer`.
- Reproduced twice (two separate tunnel launches, ports 15432 and 15433) — consistent both times.

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access / edge layer**: the edge rejected the tunnel handshake before anything reached the NAS. Likely causes: the service token (CF_ACCESS_CLIENT_ID/SECRET) is invalid, expired, or not authorized on the `db.meridiahq.com` Access application — or DNS/edge propagation for that hostname is incomplete.

Not a DB-credential problem (never got far enough to authenticate to Postgres) and not the NAS-side 192.168.0.160:5433 routing issue (that presents as connect-then-reset/timeout after a successful handshake).

## Snapshot data

None collected — no fabrication. Server version, public table count, and top-25 tables unavailable this run.

## Suggested next check

In the Cloudflare Zero Trust dashboard: confirm the service token exists and is unexpired, and that the Access application for `db.meridiahq.com` has a policy including that service token (Service Auth policy). Also confirm the hostname resolves through Cloudflare.
