# meridia_core snapshot — 2026-08-26

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run context: nightly cloud connectivity + snapshot job (fresh sandbox)
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 vars present (CF_ACCESS_CLIENT_ID/SECRET, NAS_DB_USER/PASSWORD/NAME)
- cloudflared: 2026.8.2 (Cloudflare apt pool)
- Tunnel attempt: `cloudflared access tcp --hostname db.meridiahq.com` with service-token env vars
- Result (2 attempts, ~12:44 UTC): cloudflared logged `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`; client saw connection reset on 127.0.0.1 local listener

**Failure class:** Cloudflare Access/edge layer — service token rejected or DNS/edge propagation issue for db.meridiahq.com. The failure occurs before any NAS-side or Postgres-auth layer is reached, so DB credentials and NAS routing are untested this run.

**Server version:** n/a (not reached)
**Public table count:** n/a
**Top-25 tables by live rows:** n/a

No data fabricated; no writes were made to meridia_core.
