# meridia-core snapshot — 2026-08-23

**Connection status: FAILED — NAS-side tunnel connector down/unreachable (edge 502)**

Run at 12:39 UTC from a fresh cloud sandbox (nightly connectivity job).

## Diagnosis

- Credentials fetched from Dropbox `/Claude/.env` — all 5 variables present.
- cloudflared 2026.8.2 installed from Cloudflare apt pool; `access tcp` tunnel to `db.meridiahq.com` started on 127.0.0.1:15432.
- pg8000 connect → `ConnectionResetError` (reset by peer); `cfd.log` shows `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com` (reproduced on a clean retry).
- Differential probe of the edge:
  - HTTPS **without** service token → **403** (Access policy blocks, as expected)
  - HTTPS **with** service token → **502** (Access accepts the token, then Bad Gateway)

**Interpretation:** the CF Access service token is VALID (403→502 differential proves authentication succeeds). The 502 means Cloudflare's edge cannot reach the named tunnel's origin — i.e. the cloudflared connector on the NAS is not connected to Cloudflare, or it cannot reach 192.168.0.160:5433 (consistent with cloudflared running in Docker without host networking, or the connector container being down).

**Next step (needs NAS access):** check that the cloudflared tunnel container is running on the NAS and can reach 192.168.0.160:5433; restart it if stopped.

## Data

No snapshot data — connection failed before any query ran. Server version, public table count, and top-25 tables unavailable this run.
