# meridia_core snapshot — 2026-08-27

**Connection status: FAILED — Cloudflare Access/edge layer ("websocket: bad handshake")**

- Run: nightly cloud connectivity + snapshot job (fresh cloud sandbox, 2026-08-27 ~12:43 UTC)
- Credentials: fetched OK from Dropbox /Claude/.env (all 5 vars present, non-empty)
- cloudflared 2026.8.2 installed from Cloudflare apt pool OK; pg8000 client OK
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com` with service token env vars
- Result (2 attempts, ~40s apart): `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`; pg8000 then saw `Connection reset by peer` on 127.0.0.1:15432

## Failure classification

`websocket: bad handshake` = the Cloudflare **Access/edge layer rejected the handshake** before any traffic reached the NAS. Likely causes: the CF Access **service token** (CF_ACCESS_CLIENT_ID/SECRET) is invalid, expired, or not authorized in the Access application policy for db.meridiahq.com — or DNS/edge propagation for the hostname. This is NOT a NAS-side routing failure and NOT a DB credential failure; neither was reached.

## Data

No server version, table count, or table rows collected — connection never reached Postgres. Nothing fabricated.

## Suggested check

In Cloudflare Zero Trust: verify the service token exists and is unexpired, and that the Access application for db.meridiahq.com has a policy with action "Service Auth" including that token; confirm the tunnel's public hostname config for db.meridiahq.com.
