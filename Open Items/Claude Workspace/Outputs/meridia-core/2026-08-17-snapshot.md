# meridia-core snapshot — 2026-08-17

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly connectivity + snapshot job (cloud session, fresh sandbox)
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared: 2026.8.2 (installed from pkg.cloudflare.com apt pool)
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432`
- Attempt 1 (17:24:43 UTC): `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- Attempt 2 (17:25:10 UTC, retry after 12s warmup): identical error
- Client-side symptom: pg8000 got `ConnectionResetError: [Errno 104] Connection reset by peer` on 127.0.0.1:15432 (local listener up, edge handshake rejected)

## Classification

`websocket: bad handshake` ⇒ failure at the Cloudflare Access/edge layer, before any NAS-side or Postgres involvement. Most likely causes:

1. Service token invalid, revoked, expired, or not authorized for the `db.meridiahq.com` Access application
2. DNS/edge propagation issue for `db.meridiahq.com` (record or Access app misconfigured)

Not a NAS-routing failure (that would present as connect-then-reset/timeout after a successful handshake) and not a DB credential failure (never reached Postgres auth).

## Data

No server version, table count, or row counts collected — connection never reached the database. No fabricated data.
