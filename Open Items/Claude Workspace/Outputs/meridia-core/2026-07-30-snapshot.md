# meridia_core snapshot — 2026-07-30

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly connectivity + snapshot job, fresh cloud session, 12:42–12:43 UTC.
- Credentials fetched from Dropbox `/Claude/.env`: all 5 required variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from Cloudflare apt pool; pg8000 1.31.5 installed.
- Tunnel attempt 1 (127.0.0.1:15432, 8s warmup): `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com` → client saw `ConnectionResetError: [Errno 104] Connection reset by peer`.
- Tunnel attempt 2 (127.0.0.1:15433, 12s warmup): identical `websocket: bad handshake` → same connection reset.

## Failure classification

`websocket: bad handshake` = Cloudflare Access/edge layer. The cloudflared client never established the websocket to the edge for db.meridiahq.com — this points to the service token (rejected/expired/not authorized for the Access application) or DNS/edge propagation for the hostname. It is NOT a NAS-side routing failure (that would show connect-then-reset/timeout) and NOT a DB credential failure (never reached Postgres auth).

## Snapshot data

None collected — no fabricated data. Server version, public table count, and top-25 tables unavailable this run.
