# meridia-core snapshot — 2026-09-25

Run: 2026-09-25 ~12:36 UTC, nightly scheduled job, fresh cloud session (cloudflared 2026.9.3, pg8000 1.31.5).

## Connection status: FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Credentials: all five variables present in `/Apps/Cl Cowork/Cowork-Master/.env` (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared log: `Start Websocket listener host=127.0.0.1:15432` then `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`.
- pg8000 client: `ConnectionResetError: [Errno 104] Connection reset by peer` (the local listener dropped the socket after the failed handshake).
- No database session was established. No queries ran.

## Extra diagnostic (HTTPS probe of db.meridiahq.com)

- DNS resolves to Cloudflare (2606:4700:3035::ac43:d1d9, 2606:4700:3033::6815:1754).
- Without the service token: HTTP 403 (Access blocks, as expected).
- With the service token headers: HTTP 502 `error code: 502` from Cloudflare (cf-ray a40a131babc8898c-IAD).
- Reading: the 403→502 flip means Access accepted the service token, so the token itself looks valid; the failure is behind Access, at the tunnel/origin hop (tunnel connector offline or unable to reach the Postgres origin, or the hostname's ingress rule not mapped to a tcp:// service). A plain GET against a tcp ingress can also 502, so this is suggestive, not conclusive.

## Server version

n/a (not connected)

## Public table count

n/a (not connected)

## Top 25 tables by live row count

n/a (not connected). No data fabricated.

## Suggested check for Ali

1. Cloudflare Zero Trust → Networks → Tunnels: is the tunnel serving db.meridiahq.com HEALTHY with at least one active connector?
2. Its public-hostname rule for db.meridiahq.com should be `tcp://192.168.0.160:5433`. The connector must be able to reach that address (if cloudflared runs in Docker on the NAS, use host networking).
3. Access application for db.meridiahq.com: the policy should include a Service Auth rule for this service token.
