# ASAM — Session Handoff (living)

**Last updated:** 2026-07-15. **Purpose:** let a *new* window resume without losing context — the
fix for Recess Observation 2 (Context Persistence Failure). Read this + `CLAUDE.md` + the auto-memory
`MEMORY.md` at session start. Overwrite this file with current state at the end of each session.

## How to resume (cheaply)
- **Start a fresh window** for a new task — long threads re-process all prior context every turn and
  cost more; durable knowledge does **not** live in the chat, it lives in files.
- Auto-loads in any new window: `CLAUDE.md`, the auto-memory (`MEMORY.md` + its files), and this repo.
  If this handoff isn't surfaced automatically, say: *"read docs/asam/session-handoff.md."*
- Repo is now **PRIVATE** (sensitive Meridia / Recess / Turner IP). Keep it that way.

## State as of 2026-07-15 (built this session)
- **Recess loaded to source.** `specs/recess/2026-05-24-recess-framework-package-2026.md` (loadable
  extraction of the authoritative `.docx`). Governance discipline is live.
- **Correction ledger:** appended `CL-2026-07-15-004/005/006/007` (AI drift caught + two session
  corrections promoted from memory to operative ledger + a same-session circuit-breaker recurrence).
- **SA-District design system:** `specs/design/sensory-tokens.md` +
  `specs/design/south-atlanta-district/{tokens.json, theme.css, styleguide.html, immersive-demo.html}`.
  Typography loosened to "voices with range"; immersive reading experience built.
- **Agency (B6):** `docs/agency/2026-07-15-positioning-and-first-case-study.md` (offer + SA-District case study).
- **New skill:** `.claude/skills/recess-competitive-reverse-engineer/` (first run: the Kodable + Khan scan).

## Open threads — Ali's to decide/do
- Open `immersive-demo.html` — does the feel hold? what to tune (reveal speed, drop cap, hero)?
- Exact **ivory/ink hex** from the SA-District source art (neutrals are `[CONFIRM]` placeholders).
- The **Elder's blessing** to credit Ali publicly on SA-District (unblocks the agency case study + first post).
- **Adjudicate** (Human Adjudication Memorandum): flip `CL-004` → `REVERTED` given the `CL-007`
  recurrence? Update `CL-004`'s resolution (the `.docx` is now loaded).
- Decide: adopt `roast` / `dossier` / `capture` / `patent` skills? **OKF** — OKF-ify ASAM's structure,
  and/or build Hypomone or SA-District as an OKF bundle via `arquiteto-de-empresa`.
- The **"couple more adjacent skills"** Ali still wants to discuss (raised, not yet named).

## Open threads — mine (on Ali's go)
- Convert the agency draft → one-page PDF offer + first LinkedIn post (after Elder OK).
- Package the SA-District system as a signature skill (`sad-brand-system`); feed it to Claude Design.
- Run `arquiteto-de-empresa` on SA-District (low stakes) or Hypomone.
- Prototype the multi-model orchestration layer (`relay-ai` / `fusion-fable`).
- Verify the exact VS Code extension resume UX if Ali wants it.

## Strategic anchors (don't drift)
- **Signature, not polish** (`CL-006`). **Map work to ASAM initiatives** (`CL-005`). **Load Recess to
  source before Recess work** (`CL-004`/`007`).
- First goal is **income → the agency (B6)** funds Recess, Hypomone, KSW.
