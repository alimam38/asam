# meridia_core snapshot — 2026-09-01

**Connection status:** FAIL — Cloudflare Access/edge layer (`websocket: bad handshake`)

Run from a fresh cloud sandbox at 2026-09-01 12:40 UTC via `cloudflared access tcp --hostname db.meridiahq.com` (cloudflared 2026.8.3, service-token auth from `/Claude/.env`).

## Failure detail

- `cfd.log`: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- Local pg8000 connect to 127.0.0.1:15432 → `ConnectionResetError: [Errno 104] Connection reset by peer` (local listener closed after the upstream handshake failed; the DB was never reached, so no credential test happened).
- Edge probe (curl, 15s timeout):
  - `GET https://db.meridiahq.com/` with no headers → **HTTP 403** (Access policy is enforcing).
  - Same request with `CF-Access-Client-Id/Secret` → **HTTP 502**, body `error code: 502`, `server: cloudflare`, `cf-ray: a34458f7a909f9b8-IAD`.
  - DNS resolves to Cloudflare edge (2606:4700:…), so the record is proxied correctly.

**Interpretation:** the service token is being accepted (403 without it, not 403 with it), but Cloudflare's edge cannot reach the origin behind the tunnel — a 502 on the edge→tunnel-connector hop. Most likely the `cloudflared` tunnel connector on the NAS is down / not connected, or the tunnel's public hostname `db.meridiahq.com` is not routed to a live connector. This is not a DB-credential failure and not (yet) the 192.168.0.160:5433 routing case — that case would show a successful handshake followed by a reset/timeout.

**Suggested check (NAS side):** `cloudflared tunnel info <tunnel>` / Zero Trust → Networks → Tunnels: connector status should be HEALTHY, and the public hostname `db.meridiahq.com` should map to `tcp://192.168.0.160:5433`.

## Snapshot data

Not collected — no connection. (server version: n/a · public table count: n/a · top-25 tables: n/a)
