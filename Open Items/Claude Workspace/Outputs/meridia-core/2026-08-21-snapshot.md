# meridia_core snapshot — 2026-08-21

- **Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)
- **Server version:** n/a (no DB connection established)
- **Public table count:** n/a
- **Top tables by live rows:** n/a

## Details

- Run: nightly cloud job, 2026-08-21 ~12:39 UTC, fresh sandbox.
- Credentials fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.8.2 installed from Cloudflare apt pool; `access tcp --hostname db.meridiahq.com` launched with service-token env vars.
- Two attempts (8s and 12s warmup): both failed identically. cfd.log:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- pg8000 saw `ConnectionResetError [Errno 104] Connection reset by peer` — the local listener started, but the edge rejected the websocket upgrade before any TCP path to the origin opened.

## Classification

`websocket: bad handshake` ⇒ **Cloudflare Access / edge layer**: the service token was not accepted or the `db.meridiahq.com` Access application / DNS-edge config isn't matching. This is NOT a NAS-side routing issue (that would present as connect-then-reset/timeout) and NOT a DB credentials issue (never reached Postgres auth).

Suggested checks: service token validity/expiry in Cloudflare Zero Trust, the Access application's policy includes this service token, and `db.meridiahq.com` is proxied with the tunnel's public hostname route.

No data was read or written to meridia_core.
