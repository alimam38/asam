# meridia_core snapshot — 2026-08-20

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly connectivity + snapshot job (cloud session), 2026-08-20 ~12:38 UTC
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared 2026.8.2 installed from pkg.cloudflare.com apt pool
- `cloudflared access tcp --hostname db.meridiahq.com` attempted twice (8s and 12s warmup); both attempts:
  - cfd.log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  - Client side: `ConnectionResetError [Errno 104] Connection reset by peer` on pg8000 connect to the local tunnel listener
- DNS for db.meridiahq.com resolves to Cloudflare edge (2606:4700::/32) — edge is reachable, handshake rejected there

## Failure classification

`websocket: bad handshake` ⇒ Cloudflare Access / edge layer. The request never reached the NAS-side connector. Likely causes: service token invalid/expired or not authorized by the Access application policy for db.meridiahq.com, or the Access app / DNS record config changed. Not a DB-credential issue (Postgres auth was never reached) and not NAS-side routing (that would present as connect-then-reset/timeout after a successful handshake).

## Snapshot data

None collected — no fabricated values. Server version, public table count, and top-25 tables unavailable this run.
