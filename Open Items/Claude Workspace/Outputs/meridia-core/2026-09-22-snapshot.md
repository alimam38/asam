# meridia_core snapshot — 2026-09-22

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run: nightly connectivity + snapshot job, fresh cloud session, 12:38 UTC 2026-09-22
- Credentials: fetched from Dropbox `/Apps/Cl Cowork/Cowork-Master/.env` — all 5 vars present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared 2026.9.1 installed from Cloudflare apt pool; `access tcp --hostname db.meridiahq.com` listener started OK
- Both attempts (ports 15432 and 15433, ~30s apart) failed identically:
  - cfd.log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  - pg8000 client saw `ConnectionResetError: [Errno 104] Connection reset by peer` (the local listener resetting after the edge rejected the websocket)

**Failure class:** Cloudflare Access/edge layer — service token rejected or DNS/edge propagation issue at db.meridiahq.com. The failure is *before* the tunnel connector / NAS: this is not the 192.168.0.160:5433 routing case (that presents as connect-then-timeout) and not a DB auth error (never reached Postgres).

**Server version:** n/a (not reached)
**Public table count:** n/a
**Top-25 tables:** n/a

**Suggested fix:** verify the CF Access service token (CF_ACCESS_CLIENT_ID/SECRET in the hub .env) is still valid and included in the Access policy for db.meridiahq.com, and that the Access application/DNS record for db.meridiahq.com is live.
