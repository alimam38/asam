# meridia_core snapshot — 2026-09-15

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Nightly cloud job ran at 2026-09-15 12:38 UTC from a fresh cloud sandbox.
- Credentials fetched from Dropbox `/Apps/Cl Cowork/Cowork-Master/.env` — all five vars (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME) present and non-empty.
- cloudflared 2026.9.1 installed from Cloudflare apt pool; Access TCP tunnel started for `db.meridiahq.com` → 127.0.0.1:15432.
- Two attempts (8s and 12s warm-up): both times cloudflared logged `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`, and the local pg8000 connection got `ConnectionResetError: [Errno 104] Connection reset by peer`.

**Failure class:** Cloudflare Access/edge layer — service token rejected or DNS/edge propagation for db.meridiahq.com. The tunnel never reached the origin, so this is NOT a NAS-side routing issue and NOT a DB credentials issue (neither layer was reached).

**Data:** none pulled — no snapshot data fabricated.

**Suggested checks:** validate the CF Access service token (CF_ACCESS_CLIENT_ID/SECRET) is still active and attached to the Access application for db.meridiahq.com; confirm the Access application + DNS record for db.meridiahq.com still exist and are proxied.
