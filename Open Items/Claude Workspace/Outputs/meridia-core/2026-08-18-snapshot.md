# meridia-core snapshot — 2026-08-18

- Run time: 2026-08-18 12:38 UTC (cloud scheduled job)
- Connection status: **FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)**
- Server version: n/a (never reached Postgres)
- Public table count: n/a
- Top-25 tables: n/a

## Diagnostics

- cloudflared 2026.8.2 installed from pkg.cloudflare.com apt pool; Access TCP tunnel to `db.meridiahq.com` started with service-token env vars from /Claude/.env (all 5 credentials present and non-empty).
- Two independent attempts (fresh local ports 15432 and 15433, ~10 s warm-up each). Both times cfd log shows:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  and the client saw `Connection reset by peer` — the TCP proxy never established, so no DB credentials were ever tested.
- Failure class per runbook: **Cloudflare Access/edge layer** — service token invalid/expired/not authorized for the `db.meridiahq.com` Access application, or DNS/edge propagation issue. This is NOT a NAS-side routing failure (that would present as connect-then-reset after handshake) and NOT a DB auth failure.

## Suggested next checks

1. Cloudflare Zero Trust → Access → Service Auth: confirm the service token exists, is unexpired, and is included in the policy for the `db.meridiahq.com` application.
2. Confirm `db.meridiahq.com` DNS record is proxied and the Access self-hosted app covers that hostname.
3. Re-run this job after any fix — no data was written or modified in meridia_core.
