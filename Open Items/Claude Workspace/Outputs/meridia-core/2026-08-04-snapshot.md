# meridia-core snapshot — 2026-08-04

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly cloud connectivity + snapshot job, 2026-08-04 ~12:39 UTC
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 vars present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared 2026.7.3 installed from pkg.cloudflare.com apt pool
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com` with service-token env vars
- Result (2 independent attempts, ports 15432 and 15433): cloudflared logged `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`; pg8000 client saw `ConnectionResetError(104, 'Connection reset by peer')`
- DNS: db.meridiahq.com resolves to Cloudflare edge (172.67.209.217), so DNS/proxy record is live

## Classification

`websocket: bad handshake` = the Cloudflare Access/edge layer rejected the connection **before** reaching the NAS. Since DNS resolves to Cloudflare, the likely cause is the **service token** (expired, revoked, or not included in the Access application's policy) or Access app config — not NAS-side routing and not DB credentials (the connection never reached Postgres).

## Snapshot data

None collected — no database connection was established. No queries were run; meridia_core was not touched.
