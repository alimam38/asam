# meridia_core snapshot — 2026-09-06

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run: nightly cloud connectivity + snapshot job, 2026-09-06 ~12:38 UTC (fresh cloud sandbox)
- Credentials: fetched OK from Dropbox hub .env (CF_ACCESS_CLIENT_ID/SECRET, NAS_DB_USER/PASSWORD/NAME all present)
- cloudflared 2026.8.3 installed from pkg.cloudflare.com apt pool; pg8000 1.31.5
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com` with service-token env vars
- Result (2 attempts, ~20s apart, both identical):
  - cfd.log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
  - client saw `ConnectionResetError [Errno 104] Connection reset by peer` on 127.0.0.1 local listener

**Failure class:** Cloudflare Access/edge layer — the Access edge rejected the websocket upgrade before any origin/NAS traffic. Points to the service token (revoked/expired/not authorized on the db.meridiahq.com Access application) or DNS/edge propagation for that hostname. NOT a NAS-routing failure (that presents as connect-then-reset/timeout) and NOT a DB credential failure (never reached Postgres auth).

- Server version: n/a (not connected)
- Public table count: n/a
- Top-25 tables by live rows: n/a

No data fabricated; queries were never run. Next step for Ali: verify the CF Access service token for the db.meridiahq.com application (Zero Trust > Access > Service Auth) and the Access app's policy includes it, then re-run.
