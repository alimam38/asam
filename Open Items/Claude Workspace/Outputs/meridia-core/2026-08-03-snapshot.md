# meridia-core snapshot — 2026-08-03

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run: nightly connectivity + snapshot job, fresh cloud sandbox, 12:38–12:39 UTC
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 vars present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared: 2026.7.3 (Cloudflare apt pool), `access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432`
- Result: local listener started, but both attempts (12:39:10Z and 12:39:38Z) failed with `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`. pg8000 then saw `Connection reset by peer` on 127.0.0.1:15432 — a downstream symptom of the tunnel failure, not a DB error.

**Failure class:** Cloudflare Access/edge layer — the edge rejected the tunnel handshake before any NAS-side or Postgres traffic. Likely causes: service token invalid/expired/not authorized for the `db.meridiahq.com` Access application, or DNS/edge propagation for that hostname.

**Not observed:** no NAS-side routing failure (would be connect-then-reset/timeout after a good handshake), no DB auth error (never reached Postgres).

- Server version: n/a (not connected)
- Public table count: n/a
- Top-25 tables by live rows: n/a

No data was fabricated; meridia_core was not modified (no connection was established).
