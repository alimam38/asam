# meridia_core snapshot — 2026-09-04

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run: 2026-09-04 12:39–12:40 UTC, fresh cloud session (nightly connectivity job)
- Path attempted: `cloudflared access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432` (cloudflared 2026.8.3, apt-pool install), then pg8000 to 127.0.0.1:15432
- Credentials: all five keys (CF_ACCESS_CLIENT_ID/SECRET, NAS_DB_USER/PASSWORD/NAME) present and non-empty in `/Cowork-Master/.env`
- Observed: local websocket listener started; on client connect, cloudflared logged `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`; pg8000 saw `ConnectionResetError: [Errno 104] Connection reset by peer`
- Reproduced twice in the same run (clean relaunch, same result)

**Failure class:** Cloudflare Access/edge layer — service token rejected or DNS/edge propagation for `db.meridiahq.com`. The failure occurs before any NAS-side or Postgres-side involvement, so DB credentials and NAS routing are untested this run.

Server version: n/a (not reached)
Public table count: n/a
Top-25 tables: n/a

_No data fabricated; diagnostic run._
