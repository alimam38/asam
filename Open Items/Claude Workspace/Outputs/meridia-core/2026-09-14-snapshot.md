# Meridia Core Snapshot — 2026-09-14

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

**Run details (nightly cloud job, 12:38–12:40 UTC):**
- Credentials fetched from hub .env: all five variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.9.1 installed from Cloudflare apt pool; Access TCP tunnel launched with service token toward db.meridiahq.com.
- Two clean, independent attempts both failed identically: cfd.log shows `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`, and the client connection to the local forwarder was reset (Errno 104).

**Failure classification (per runbook):** websocket: bad handshake ⇒ Cloudflare Access/edge layer — service token not accepted by the Access application, or DNS/edge propagation issue for db.meridiahq.com. This is upstream of the NAS and upstream of Postgres auth: no database connection was ever established.

**What this rules out:**
- Not a NAS-side routing failure (that presents as connect-then-reset/timeout after a successful handshake).
- Not a DB credential problem (never reached Postgres auth).

**Suggested fix (for Ali):** verify the Access application for db.meridiahq.com still lists this service token (CF_ACCESS_CLIENT_ID in hub .env) in its policy, that the token has not expired or been rotated, and that the db.meridiahq.com hostname/route still resolves at the Cloudflare edge.

**Snapshot data:** none — connection failed; no server version, table count, or row counts collected. Nothing fabricated.
