# meridia_core snapshot — 2026-09-12

Run: 2026-09-12 12:38 UTC (nightly cloud job)

## Connection status: FAILED — Cloudflare edge → NAS tunnel connector

- Failure class: tunnel connector unreachable (NAS side), NOT a token or DB-credential problem.
- Evidence:
  - `cloudflared access tcp` → `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`; local client then got `Connection reset by peer` (reproduced twice, fresh ports).
  - Direct probe of https://db.meridiahq.com WITHOUT service token → **403** (Access policy enforcing normally).
  - Same probe WITH the CF service token → **502 Bad Gateway** from Cloudflare (cf-ray a39ef9ddd8c62e05-IAD).
  - 403→502 flip proves the service token is accepted by Access; Cloudflare's edge then cannot reach the tunnel origin.
- Interpretation: the NAS-side cloudflared tunnel connector is down or cannot reach 192.168.0.160:5433 (e.g. container stopped, or cloudflared in Docker without host networking).
- Fix is on the NAS: confirm the cloudflared tunnel container is running and healthy (`cloudflared tunnel info`), and that its ingress can reach 192.168.0.160:5433.

## Snapshot data

None captured — no fabricated values. Server version, public table count, and top-25 tables were not retrievable this run.

## Environment notes

- Credentials fetched from hub .env: all 5 keys present (CF id/secret, DB user/password/name).
- cloudflared 2026.9.1 installed from Cloudflare apt pool; pg8000 1.31.5.
