# meridia-core snapshot — 2026-07-28

**Connection status:** FAILED — Cloudflare edge layer (tunnel connector down/unreachable)

## Diagnosis

- Credentials fetched from Dropbox `/Claude/.env`: all 5 variables present.
- cloudflared 2026.7.3 installed from Cloudflare apt pool; Access TCP proxy started on 127.0.0.1:15432.
- Two connection attempts, both failed identically: `websocket: bad handshake` in cfd.log; pg8000 got `ConnectionResetError [Errno 104]`.
- Edge probe (curl to https://db.meridiahq.com):
  - **Without** service token → HTTP 403 (Access is enforcing the policy).
  - **With** service token → HTTP 502 (token accepted; Bad Gateway from the edge).

**Conclusion:** The CF Access service token is valid and authentication passes. The failure is that Cloudflare's edge cannot reach the tunnel origin for db.meridiahq.com — the NAS-side cloudflared connector is down, not connected, or not serving this hostname (HTTP 502). This is NOT a service-token problem and NOT a DB-credential problem. Check that the cloudflared connector on the NAS is running and the tunnel shows HEALTHY in the Zero Trust dashboard.

## Snapshot data

None collected — no fabricated data. Server version, public table count, and top-25 tables unavailable until the tunnel connector is restored.

---
*Nightly connectivity + snapshot job, run from a fresh cloud session at 2026-07-28T12:40Z.*
