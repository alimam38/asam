# meridia-core snapshot — 2026-07-23

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

- Run time: 2026-07-23 ~12:40 UTC (nightly cloud job)
- Tunnel: `cloudflared 2026.7.3 access tcp --hostname db.meridiahq.com` from a fresh cloud sandbox
- Credentials: fetched OK from Dropbox `/Claude/.env` (all 5 vars present); service token env vars were set
- cloudflared log (2 independent attempts, ~13s apart, identical result):
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- Client-side symptom: local listener up, but TCP connection reset by peer on first byte

**Failure class:** Cloudflare Access / edge layer — the Access edge rejected the websocket upgrade before any traffic reached the tunnel connector. Likely causes: service token invalid/expired/not authorized for the `db.meridiahq.com` Access application, or DNS/edge propagation for that hostname.

Not a DB-credential issue (never got far enough to authenticate to Postgres) and not the NAS-side 192.168.0.160:5433 routing class (that presents as connect-then-reset/timeout after a successful handshake).

No data captured this run — server version, public table count, and top-25 tables unavailable. No writes were made to meridia_core.
