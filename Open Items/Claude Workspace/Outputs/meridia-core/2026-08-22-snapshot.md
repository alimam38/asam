# meridia-core snapshot — 2026-08-22

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Run: nightly cloud connectivity + snapshot job, 2026-08-22 ~12:39 UTC
- Credentials: fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME)
- cloudflared 2026.8.2 installed from pkg.cloudflare.com apt pool
- Tunnel: `cloudflared access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432` with service-token env vars set
- Result: cloudflared listener started, but every origin connection attempt failed with `websocket: bad handshake` (two attempts on a clean tunnel process, ~10s apart). pg8000 saw `Connection reset by peer` on the local socket — consistent with cloudflared tearing down the proxied connection after the edge rejected the handshake.

## Failure classification

`websocket: bad handshake` = rejected at the Cloudflare Access/edge layer, **before** reaching the NAS. Likely causes: service token invalid/expired/not authorized for the `db.meridiahq.com` Access application, or DNS/edge propagation for the hostname. This is NOT a NAS-side routing failure (that would present as connect-then-reset/timeout) and NOT a DB credential failure (no Postgres auth exchange ever occurred).

## Data

No data pulled — server version, table count, and top-25 tables unavailable this run. Nothing was fabricated; the database was never reached and was not modified.
