# meridia-core snapshot — 2026-08-25

- **Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)
- **Failure class:** The cloudflared Access client reached the edge but the WebSocket upgrade to origin `https://db.meridiahq.com` was rejected. Reproduced twice (12:40:08 and 12:40:32 UTC) with fresh tunnels on different local ports. This points to the Cloudflare Access layer — service-token rejection (expired/revoked/not authorized for the app) or DNS/edge propagation for `db.meridiahq.com` — NOT the NAS-side connector and NOT DB credentials (the TCP path never opened, so Postgres auth was never attempted).
- **Server version:** n/a (not reached)
- **Public table count:** n/a
- **Top-25 tables by live rows:** n/a

## Run detail
- Credentials fetched from Dropbox `/Claude/.env`: all 5 vars present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.8.2 installed from pkg.cloudflare.com apt pool.
- pg8000 1.31.5; local connect to the tunnel listener returned `ConnectionResetError [Errno 104]` after the edge handshake failed.
- cfd.log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`

## Suggested next checks
1. Cloudflare Zero Trust → Access → Service Auth: confirm the service token exists, is not expired, and is included in the Access policy for the `db.meridiahq.com` application.
2. Confirm the Access application for `db.meridiahq.com` still exists and the hostname's DNS record is proxied (orange-cloud).
3. If the token was rotated, update `/Claude/.env` in Dropbox.
