# meridia_core nightly snapshot — 2026-09-08

**Connection status: FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)**

- Run: cloud session, 2026-09-08 ~12:39 UTC
- cloudflared 2026.8.3 installed from Cloudflare apt pool; tunnel client started OK, service token env vars set from hub .env.
- Two attempts, both failed identically: cloudflared logged `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`, and the local pg8000 connection to 127.0.0.1 was reset by peer.
- Additional diagnostic: an authenticated HTTPS probe of https://db.meridiahq.com (with CF-Access service-token headers) returned **HTTP 502** from the Cloudflare edge — consistent with the edge accepting the request but the NAS-side tunnel connector being down or unreachable, rather than a plain DNS miss.

**Failure class:** Cloudflare Access/edge layer (service token or DNS/edge propagation, per runbook). The 502 suggests checking that the cloudflared tunnel connector on the NAS is running and healthy before suspecting the service token.

No database data was retrieved; server version, table count, and top-25 tables are unavailable for this run. No writes were made to meridia_core.
