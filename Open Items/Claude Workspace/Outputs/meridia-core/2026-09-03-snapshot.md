# meridia_core snapshot — 2026-09-03

**Connection status: FAILED — Cloudflare tunnel origin unreachable (NAS-side)**

## What happened

Nightly cloud job attempted a READ-ONLY connection to meridia_core via `cloudflared access tcp --hostname db.meridiahq.com` (local forward 127.0.0.1:15432). Two clean attempts, both failed identically:

- cloudflared log: `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- pg8000 client saw: `ConnectionResetError: [Errno 104] Connection reset by peer`

## Failure classification (refined by edge probes)

HTTP probes against `https://db.meridiahq.com`:

| Probe | Result |
|---|---|
| No service token | **403** (Access correctly blocking) |
| With CF_ACCESS_CLIENT_ID/SECRET headers | **502 Bad Gateway** |
| DNS resolution | OK (172.67.209.217, Cloudflare edge) |

Interpretation: the service token in `.env` is **accepted** by Cloudflare Access (403 without it, past-Access 502 with it), and DNS/edge propagation is fine. The 502 means Cloudflare's edge cannot reach the origin behind the tunnel — the NAS-side `cloudflared` tunnel connector is down/disconnected from Cloudflare, or it cannot reach the Postgres origin at 192.168.0.160:5433 (consistent with the known risk of cloudflared running in Docker without host networking).

**Action needed (Ali):** check the tunnel connector on the NAS — is the `cloudflared` tunnel container running and showing a healthy connection in the Cloudflare Zero Trust dashboard, and can it reach 192.168.0.160:5433 from inside its network namespace?

## Data

No snapshot data pulled — connection never reached Postgres. Server version, table count, and top-25 tables unavailable this run. Nothing fabricated.

---
*Job: nightly meridia-core connectivity + snapshot (cloud session, 2026-09-03 12:39 UTC). Read-only; no writes attempted against meridia_core.*
