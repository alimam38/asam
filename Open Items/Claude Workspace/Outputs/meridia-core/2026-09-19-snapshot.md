# meridia-core snapshot — 2026-09-19

**Connection status: FAILED — Cloudflare Access/edge layer**

- Run: 2026-09-19 12:37–12:39 UTC, fresh cloud session (nightly connectivity job)
- Failure class: `websocket: bad handshake` from cloudflared when dialing origin `https://db.meridiahq.com` — the Cloudflare Access/edge layer rejected the tunnel handshake (service-token auth or DNS/edge propagation). This is NOT a NAS-side routing failure (that presents as connect-then-reset/timeout) and NOT a DB credential failure (never reached Postgres auth).
- Reproduced twice with independent tunnel processes; identical error both times.
- cfd.log (attempt 2):
  ```
  2026-09-19T12:38:53Z INF Start Websocket listener host=127.0.0.1:15432
  2026-09-19T12:39:06Z ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
  ```
- Preconditions verified OK: .env fetched from Dropbox; CF_ACCESS_CLIENT_ID / CF_ACCESS_CLIENT_SECRET / NAS_DB_USER / NAS_DB_PASSWORD / NAS_DB_NAME all present and non-empty; cloudflared 2026.9.1 installed from pkg.cloudflare.com; pg8000 installed.
- Server version: n/a (no connection)
- Public table count: n/a
- Top-25 tables by live rows: n/a

**Next action (Ali):** check the Cloudflare Access application for db.meridiahq.com — service token validity/expiry and that the token is attached to the Access policy; also confirm the db.meridiahq.com DNS record still points at the tunnel.
