# meridia_core snapshot — 2026-07-31

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Job ran from a fresh cloud sandbox at ~12:42–12:43 UTC, 2026-07-31.
- Credentials fetched from Dropbox `/Claude/.env`: all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from Cloudflare apt pool; pg8000 1.31.5 installed.
- `cloudflared access tcp --hostname db.meridiahq.com` attempted twice (8s and 12s warm-up). Both attempts logged:
  `ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com`
- The local Postgres connection to 127.0.0.1:15432 was reset by peer (tunnel never established), so no queries ran.

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access / edge layer** — the service token was rejected or the `db.meridiahq.com` Access application/DNS edge config is not accepting the connection. This is NOT a NAS-side routing issue (that would present as connect-then-reset/timeout) and NOT a DB credential issue (the tunnel never reached Postgres).

## Next checks

1. Verify the Access service token (CF_ACCESS_CLIENT_ID/SECRET) is still valid and attached to the `db.meridiahq.com` Access application policy.
2. Confirm the Access application for `db.meridiahq.com` (self-hosted, TCP) exists and DNS routes through the tunnel.
3. Re-run this job after fixing — no data was fabricated; server version, table count, and top-25 tables were NOT collected this run.
