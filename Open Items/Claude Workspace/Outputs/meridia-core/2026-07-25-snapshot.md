# meridia-core snapshot — 2026-07-25

**Connection status:** FAILED — Cloudflare Access/edge layer (`websocket: bad handshake`)

## Details

- Job ran from a fresh cloud sandbox at 2026-07-25 ~12:39 UTC.
- Credentials fetched from Dropbox `/Claude/.env` — all 5 variables present (CF_ACCESS_CLIENT_ID, CF_ACCESS_CLIENT_SECRET, NAS_DB_USER, NAS_DB_PASSWORD, NAS_DB_NAME).
- cloudflared 2026.7.3 installed from Cloudflare apt pool; pg8000 client installed.
- Tunnel launched: `cloudflared access tcp --hostname db.meridiahq.com --url 127.0.0.1:15432` with service-token env vars set.
- Postgres connect attempt via 127.0.0.1:15432 → `ConnectionResetError: [Errno 104] Connection reset by peer`.
- cfd.log (both attempts):
  ```
  INF Start Websocket listener host=127.0.0.1:15432
  ERR failed to connect to origin error="websocket: bad handshake" originURL=https://db.meridiahq.com
  ```
- Retried once with a longer warm-up (12s): identical failure — persistent, not transient.

## Failure classification

`websocket: bad handshake` ⇒ **Cloudflare Access / edge layer**: the edge rejected the tunnel handshake before any NAS-side or DB-side code ran. Most likely the service token (CF_ACCESS_CLIENT_ID/SECRET) is invalid, expired, or not authorized for the `db.meridiahq.com` Access application; alternatively DNS/edge propagation for the hostname.

Not a NAS-routing failure (that would present as connect-then-reset after a successful handshake) and not a DB credential failure (never reached Postgres auth).

## Snapshot data

None collected — no server version, table count, or row counts available this run. No data fabricated.
